/**
 * Define the REST API for Retrofit via the following interface and
 * create a Retrofit client.
 * <p>
 * Provides the various interfaces containing all the necessary CShare API endpoints. The instantiation of these interfaces is done via a Retrofit object (built in
 * {@link com.example.cshare.webservices.NetworkClient} class) which allows to build a
 * Retrofit service, used to make the calls.
 *
 * @since 1.0
 * @author Clara Gros
 * @author Babacar Toure
 */

package com.example.cshare.webservices ;